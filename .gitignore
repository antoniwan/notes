# build output
dist/

# Social JPEG/PNG under public/social are build artifacts but must be tracked (or CI/Vercel
# recomputes from AVIF each time — very slow). Fingerprint metadata:
# src/data/socialImageFingerprints.json + src/data/socialImageManifest.ts

# generated types
.astro/
.vercel/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log


# environment variables / local secrets (never commit)
# Includes Vercel CLI tokens written to .env.local (e.g. VERCEL_OIDC_TOKEN)
.env
.env.production
.env.local
.env*

# additional build outputs
build/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# private generated strategy reports
docs/materials/*
!docs/materials/.gitkeep

# graphify knowledge-graph cache (regenerate with `graphify update .`)
graphify-out/
