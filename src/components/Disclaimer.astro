---
import { DISCLAIMER_TEXT } from '../consts';

interface Props {
  text?: string;
  class?: string;
  style?: string;
}

const { text = DISCLAIMER_TEXT, class: className = '', style = '' } = Astro.props;
---

<div
  class:list={[
    'bg-[rgb(var(--color-bg-alt))]/50',
    'border-l-4 border-[rgb(var(--color-primary))]',
    'p-element md:p-block',
    'my-block',
    'rounded-md',
    'shadow-xs',
    'theme-transition',
    className,
  ]}
  role="complementary"
  aria-label="Disclaimer"
  style={style}
>
  <div class="mb-2 flex items-center gap-2">
    <span class="text-xl" aria-hidden="true">🌟</span>
    <span class="text-lg leading-tight font-semibold text-[rgb(var(--color-text))]"
      >Sacred Disclaimer</span
    >
  </div>
  <p class="text-body-muted m-0 leading-relaxed" set:html={text} />
</div>
